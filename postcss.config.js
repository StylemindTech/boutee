/** @type {import('postcss-load-config').Config} */
export default {
  // Explicitly empty to stop PostCSS from walking up to a parent config that references a missing tailwind install.
  plugins: {},
};
